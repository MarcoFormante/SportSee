export function PerformanceContainer({children}:{children:React.ReactNode}){
    return (
        <div className="bg-[#FFFFFF] w-full h-full rounded-[10px]">
            {children}
        </div>
    )
}